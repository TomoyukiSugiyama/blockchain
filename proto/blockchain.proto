syntax = "proto3";
option go_package = "./proto";

service Blockchain {
  // Sends a greeting
  rpc SayHello (Request) returns (Reply) {}
}

message Request {
    string op = 1;
}

message Reply {
    string message = 1;
}
